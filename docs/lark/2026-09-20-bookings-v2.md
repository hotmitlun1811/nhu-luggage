# Bảng "Bookings v2": mỗi thông tin của form có một cột riêng

> **Trạng thái (2026-09-20): đã bật.** Chủ cửa hàng đổi 2 biến ở Vercel lúc 14:03 (`LARK_BASE_TABLE_ID` sang bảng này, `LARK_BOOKINGS_SCHEMA=2`); commit `8d669b4` deploy xong lúc khoảng 14:11 (bản `nhu-luggage-56smvgy19`), site `www.stowdanang.com` chạy bình thường. **Chưa có đơn thật nào ghi thẳng vào bảng này để xác nhận**: hai biến là loại `sensitive` nên không đọc lại được giá trị, chỉ một đơn thật đi qua mới chứng minh được (mục 9, bước 4). Bảng có 42 dòng đã chuyển (41 lúc đầu + 1 đơn `STW-260920-FVFHMK` phát sinh ở bảng cũ trong lúc chờ deploy, mục 8).
>
> **Tên bảng (đổi ngày 2026-09-20):** bảng đang dùng là **`Bookings`** (id `tblphiUeqDjjPdRL`, trước đây tên `Bookings v2 (test)`). Bảng cũ đổi tên thành **`Bookings (old, archived)`** (id `tblTGHAMqIRiCOgd`), giữ nguyên toàn bộ dữ liệu để truy soát. `Test` giữ nguyên tên. Các đoạn bên dưới viết trước lúc đổi tên có thể còn dùng tên cũ.
> Bảng mới có view **`Active`** (Status là Booking, Confirm hoặc Paid; giờ lấy mới nhất trước) thay cho `Grid View` của bảng cũ. Các dòng thử của tôi đã xóa.
>
> Người đọc: chủ cửa hàng. Trong cùng Base "Stow Bookings". Script tạo lại: [`create-bookings-v2.py`](create-bookings-v2.py).

## 1. Vì sao cần bảng mới

Bảng cũ được xây từ form đời đầu rồi vá thêm. Nhiều thứ form hiện tại thu thập hoặc tính ra không có chỗ để lưu, hoặc lưu lệch:

| Vấn đề ở bảng cũ | Bảng mới |
|---|---|
| Giờ gửi và giờ lấy nằm ở **hai cột** (ngày là mốc 12:00, giờ là chữ) | Mỗi cái **một cột ngày-giờ** (`Drop-off`, `Pick-up`), giờ Việt Nam chính xác |
| Không lưu **sự đồng ý điều khoản** (chỉ nằm trong tin WhatsApp) | `Terms Agreed`, `Terms Agreed At`, `Terms Version` |
| `Duration` ghi nhãn gói (`1 month`, `Custom: ...`), không cho biết khách gửi thật bao lâu | `Duration` là thời gian thật từ gửi đến lấy (`46 days`, `23 hours`) |
| Không lưu **số bag cỡ lớn** (chỉ có ô tick) | `Oversized Bags` là số |
| Không lưu **giá mỗi bag** và **phụ phí** | `Price per Bag`, `Oversized Surcharge` |
| Không lưu **nước của số WhatsApp** khách chọn | `Phone Country` |
| Cột `Date` là công thức phải đoán ngày hết hạn từ chữ trong Duration | Bỏ. Form đã bắt buộc chọn giờ lấy nằm trong hạn của gói, nên `Pick-up` là ngày cần biết |
| Cột lộn xộn: `Extand`, `Extand1`, `Extand 2` | Chỉ giữ ý nghĩa rõ ràng: `Extension Fee` |

## 2. Các cột (30)

| Cột | Kiểu | Ai điền | Ghi chú |
|---|---|---|---|
| **Reference** | text (cột chính) | form | `STW-YYMMDD-XXXXXX`, ví dụ `STW-260921-89KCPD`: `YYMMDD` là **ngày gửi** (không phải ngày đặt), `XXXXXX` là **6 ký tự ngẫu nhiên** (không nguyên âm, không 0/O/1/I/L) nên ghi được lên thẻ hành lý. Gần như không thể trùng, xem mục 7 |
| Status | chọn | form, rồi nhân viên | Đơn mới là `Booking`. Cùng 5 giá trị và màu như bảng cũ |
| Source | chọn | form | `Booking Form` hoặc `Intake` (form nhân viên) |
| Submitted at | ngày-giờ | form; dòng tạo tay tự điền giờ tạo | Là cột ngày-giờ thường (không phải cột hệ thống) để dòng chuyển từ bảng cũ giữ được giờ khách đặt thật |
| Lane | chọn | form | `Flexible`, `Flat Rate`, `Custom` |
| Plan | chọn | form | 5 gói và `Custom` |
| **Drop-off** | ngày-giờ | form | |
| **Pick-up** | ngày-giờ | form | Form đảm bảo nằm trong hạn của gói. Form nhân viên không có nên để trống |
| **Plan End** | ngày-giờ | form | Thời điểm gói khách đã trả tiền kết thúc. Khách có thể dời giờ lấy tới bất cứ lúc nào trước mốc này mà không phải trả thêm (và thêm 60 phút ân hạn). Do form tính bằng bộ máy tính giá, xem mục 10. Khi khách trả thêm để gia hạn, nhân viên sửa cột này. Form nhân viên không có nên để trống |
| **Duration** | text | form | Thời gian thực tế từ `Drop-off` đến `Pick-up`, code tính sẵn rồi ghi: `46 days`, `23 hours`, `1 day 7 hours`, `1 hour 30 minutes` (phần bằng 0 bỏ qua). Không có nhãn gói hay ghi chú. Để trống khi chưa có giờ lấy (form nhân viên). Không phải công thức Lark: nếu nhân viên sửa `Pick-up` trong Lark thì cột này không tự đổi |
| Bags | số | form | |
| **Oversized Bags** | số | form | 0 = không có |
| **Price per Bag** | số | form | Giá thuê 1 bag, chưa gồm phụ phí |
| **Oversized Surcharge** | số | form | Tổng phụ phí của mọi bag cỡ lớn |
| Total (VND) | số | form | Giá form báo |
| Discount | số | nhân viên | |
| Extension Fee | số | nhân viên | Tiền thu thêm khi khách gia hạn. Trước là `Extand` |
| Thực nhận | công thức | Lark | `Total (VND) − Discount + Extension Fee`, cùng công thức bảng cũ |
| Name | text | form | |
| WhatsApp | text | form | Dạng `+84905955161` |
| Email | text | form | |
| **Phone Country** | text | form | Mã 2 chữ (VN, IT, JP...) |
| **Terms Agreed** | ô tick | form | |
| **Terms Agreed At** | ngày-giờ | form | Bằng chứng khách đã đọc hết điều khoản |
| **Terms Version** | text | form | Ngày hiệu lực của điều khoản |
| Price Detail | text | form | Từng bước tính giá, giống bảng dưới tổng tiền |
| Note | text | nhân viên | |
| **Old Record ID** | text | script chuyển dữ liệu | Chỉ dòng chuyển từ bảng cũ: mã dòng gốc. Để chuyển lại không nhân đôi và đối chiếu từng dòng |
| **Migration Note** | text | script chuyển dữ liệu | Chỉ dòng chuyển từ bảng cũ: điều đáng biết về dòng đó (ví dụ giờ mà bảng cũ không ghi). Trống nếu không có gì |
| **Row No.** | số tự tăng | Lark | `00001`, `00002`... Lark cấp, **không bao giờ lặp**, kể cả khi hai dòng dùng chung một Reference (ví dụ dòng gia hạn) |

In đậm = cột mới so với bảng cũ. WhatsApp và Email là text thường, cố ý không dùng kiểu "điện thoại/email" của Lark: nếu Lark từ chối một giá trị thì cả dòng đặt chỗ bị mất, và tôi không muốn mất đơn vì lý do đó.

## 3. Cách hoạt động

Route `/api/lark/booking` chọn kiểu ghi theo biến môi trường `LARK_BOOKINGS_SCHEMA`:
- **để trống** (mặc định): ghi vào bảng cũ như hiện nay.
- **`2`**: ghi vào bảng mới. Khi đó `LARK_BASE_TABLE_ID` phải là id bảng mới.

Hai biến này luôn đổi cùng nhau. Form nhân viên (`/intake`) đi qua cùng route nên cũng ghi vào bảng mới, chỉ điền các cột nó có.

## 4. Đã thử (chỉ ghi vào bảng mới, tin nhắn nhóm chat bị chặn không gửi)

3 dòng thử, tên chứa "safe to delete", bạn xóa được: `STW-260921-6338` (By the Day), `STW-260921-8072` (Custom 46 ngày, 1 bag cỡ lớn), `STW-260921-INTK` (form nhân viên). Đã kiểm tra từng ô: giờ lưu đúng `+07:00`, công thức Thực nhận đúng, dòng nhân viên chỉ có cột nào có dữ liệu (Duration để trống vì chưa có giờ lấy), số `0049…` thành `+49…`. Sau đó đổi cách ghi Duration thành thời gian thật và cập nhật 3 dòng thử cho khớp.

## 5. Khi bạn muốn dùng thật

1. Xem bảng và xóa 3 dòng thử. Đổi tên bảng thành `Bookings v2` (bỏ chữ test) nếu bạn thích.
2. Commit và push code (có `buildBookingFieldsV2` và các trường mới của form).
3. Ở Vercel, đặt `LARK_BASE_TABLE_ID=tblphiUeqDjjPdRL` và `LARK_BOOKINGS_SCHEMA=2`, rồi deploy lại (đổi biến môi trường chỉ có hiệu lực sau lần deploy tiếp theo). Máy của bạn (`.env.local`) đổi tương tự nếu muốn thử ở local.
4. Từ đó đơn mới vào bảng mới; nhân viên làm việc ở bảng mới. **Bảng cũ và toàn bộ dữ liệu cũ giữ nguyên**, chỉ không nhận đơn mới nữa.

## 6. Tôi tự quyết, cần bạn xem lại

- **Không mang theo** `Extand1` (ô tick) và `Extand 2` (chọn Flexible/Flat Rate): tôi không biết chắc ý nghĩa. `Extand` được hiểu là tiền gia hạn nên thành `Extension Fee`.
- **Không tạo view** "Active" như `Grid View` cũ của bạn (lọc Status là Booking hoặc Paid). Làm được nếu bạn muốn.
- **Không chuyển dữ liệu cũ** sang bảng mới, theo yêu cầu của bạn. Nếu cần lịch sử ở cùng một chỗ, tôi có thể viết phép chuyển (đọc bảng cũ, ghi bảng mới, không sửa bảng cũ).
- Tên các cột khác với bảng cũ (`Drop-off Date` + `Drop-off Time` thành `Drop-off`, `Pax` thành `Bags`, `Oversized` thành `Oversized Bags`). Bảng thống kê hoặc dashboard nào đang dựa vào tên cũ sẽ không dùng được với bảng mới.

## 7. Mã Reference: đã đổi để gần như không thể trùng

Mã cũ là `STW-` + ngày gửi + 4 chữ số ngẫu nhiên (`Math.random`, chỉ 9.000 khả năng mỗi ngày). Không có nơi nào kiểm tra trùng, và cột chính của Lark cũng không bắt buộc duy nhất. Với 10 đơn gửi cùng ngày, xác suất trùng là 0,5%.

**Mã mới** (`src/lib/reference.ts`, dùng chung cho form khách, form nhân viên và route): `STW-260921-89KCPD`.
- 6 ký tự lấy từ 27 ký tự: số 2-9 và các phụ âm B C D F G H J K M N P Q R S T V W X Z. Bỏ nguyên âm nên mã **không bao giờ tạo thành một từ** trên thẻ khách cầm. Bỏ 0/O và 1/I/L nên khó đọc nhầm khi viết tay hay đọc to.
- 27^6 = **387 triệu mã mỗi ngày gửi**. Tạo bằng bộ ngẫu nhiên an toàn của trình duyệt (`crypto`), bỏ các giá trị sẽ làm lệch xác suất nên mọi ký tự đều như nhau.

| Số đơn gửi cùng một ngày | Xác suất trùng ít nhất một cặp | Mã cũ (4 chữ số) |
|---|---|---|
| 3 | 1 trên 129 triệu | 0,03% |
| 10 | 1 trên 8,6 triệu | 0,5% |
| 20 | 1 trên 2 triệu | 2,1% |
| 100 | 1 trên 78.000 | quá 50% |

Với 10 đơn mỗi ngày, trung bình khoảng **23.000 năm** mới trùng một lần.

**Không thể đảm bảo tuyệt đối** bằng một mã do trình duyệt tạo, vì không có bộ đếm chung. Vì vậy bảng mới có thêm cột **`Row No.`** (số tự tăng của Lark): Lark bảo đảm không bao giờ lặp. Nếu sau này cần phân biệt chắc chắn hai dòng, dùng `Row No.`.

- Mã cũ trong bảng cũ giữ nguyên. Trang web đã mở sẵn từ trước khi cập nhật có thể còn gửi vài mã kiểu cũ trong một thời gian ngắn.
- Nếu muốn chắc hơn nữa: cho route tra Lark xem mã đã có chưa rồi cảnh báo nhóm chat, cũng bắt được trường hợp một đơn bị gửi hai lần như `STW-260827-7038` trong bảng cũ (hai dòng giống hệt, một dòng bị hủy ghi "Trùng"). Chưa làm.

## 8. Chuyển dữ liệu cũ sang bảng này (đã làm ngày 2026-09-20)

Nguồn: bảng `Bookings` (**chỉ đọc**, không bị sửa). Đích: bảng này. Script [`migrate-to-v2.py`](migrate-to-v2.py) (mặc định chỉ thử khô; `--apply` để ghi; `--verify` để đối chiếu) và 21 test của các quy tắc chuyển ([`test_migrate_to_v2.py`](test_migrate_to_v2.py)).

**Kết quả:** 41 trong 43 dòng được chuyển. 2 dòng bỏ qua vì **không có dữ liệu nào** ngoài giờ tạo tự động: `recvvDKMvAJICK` và `recvvDKUgPXw9i` (cùng tạo ngày 19/9 lúc 12:14). Đối chiếu từng dòng với nguồn: 779 giá trị đều khớp; tổng `Total (VND)` 17.765.000, `Discount` 2.805.000 và `Extension Fee` 615.000 đều khớp từng đồng; số dòng theo Status và theo Plan khớp. Chạy lại script thì báo "đã chuyển 41, cần chuyển 0". Bảng `Bookings` (rev 386) và `Test` (rev 13) không đổi. Bản gốc lưu ở `~/stow-lark-backup/migration-2026-09-20/`, kèm `reconciliation.csv` để bạn tự mở đối chiếu từng dòng (không có tên, số điện thoại hay email).

**Bảng cũ → bảng mới:**

| Bảng cũ | Bảng mới | Cách chuyển |
|---|---|---|
| Reference, Status, Source, Lane, Plan, Name, Email, Note | cùng tên | Giữ nguyên từng chữ |
| Phone | WhatsApp | Giữ nguyên, **không đoán mã quốc gia** |
| Submitted at | Submitted at | Giờ khách đặt thật (không phải giờ chuyển) |
| Drop-off Date + Drop-off Time | Drop-off | Ngày cộng giờ. Không có giờ thì 00:00 và ghi trong Migration Note |
| Pickup Date + Pickup Time | Pick-up | Như trên |
| Duration | Duration | Thời gian thật giữa hai mốc, **chỉ khi biết cả hai giờ**. Nhãn cũ (ví dụ `1 day`) giữ trong Migration Note nếu khác |
| Pax, Oversized Count | Bags, Oversized Bags | |
| Total (VND), Discount | cùng tên | |
| Extand | Extension Fee | |
| Thực nhận (công thức) | Thực nhận | Công thức mới, kết quả khớp |
| Extand1 (ô tick) | Migration Note | Chỉ 1 dòng: `STW-260821-7499` |
| Extand 2, Date | bỏ | `Extand 2` trùng `Lane` ở mọi dòng; `Date` thay bằng `Pick-up` |
| (bảng cũ không có) | Price per Bag, Oversized Surcharge, Phone Country, Terms Agreed/At/Version, Price Detail | **Để trống**, không điền bịa |

**Những điều cần bạn xem** (đều đã ghi trong cột `Migration Note` của dòng đó):
- **12 dòng không có giờ lấy** (gói Mini, Strand, Long Stay, các đơn By the Day đầu tiên, và 2 dòng nhân viên nhập `Noname`, `Noname2`; hai dòng `Noname` còn không có cả giờ gửi): giờ được đặt 00:00. Cửa hàng mở từ 07:00 nên 00:00 chỉ có thể là "chưa biết giờ". Cột `Duration` của các dòng này để trống.
- **`STW-260917-1331`** (Long Stay, 6.000.000 ₫): Note ghi "02 oversize" nhưng ô tick oversized chưa bật. Tôi giữ `Oversized Bags = 0` theo dữ liệu có cấu trúc và gắn cờ, không tự sửa. Cần bạn xác nhận số bag cỡ lớn thật.
- **7 số WhatsApp thiếu mã quốc gia**: giữ nguyên, có cờ "no country code" (chờ bạn xác nhận mã nước).
- **2 mã Reference xuất hiện 2 lần** (`STW-260812-2784`, `STW-260827-7038`): cả hai dòng đều được chuyển, phân biệt bằng `Row No.`.
- **`Duration` khác nhãn cũ**: cột cũ ghi số giờ tính tiền (làm tròn lên), cột mới ghi thời gian thật. Ví dụ `STW-260810-7795` cũ `3 hours`, mới `2 hours 30 minutes`; nhãn cũ có trong Migration Note.

**Đối chiếu sau khi đã dùng thật:** nhân viên sửa Status, Note, Discount... ngay trong bảng này (ví dụ ngày 2026-09-20 "Hồ Như" đổi Status của `STW-260919-3280` và `STW-260919-2514` từ Booking sang Complete lúc 13:26), còn bảng cũ đã đóng băng, nên các cột đó sẽ luôn "khác". Đó không phải lỗi chuyển dữ liệu (lịch sử sửa của Lark ghi rõ ai sửa, lúc nào). Dùng `python3 docs/lark/migrate-to-v2.py --verify --ignore "Status,Note,Discount,Extension Fee,Received (Thực nhận)"` để đối chiếu mọi cột còn lại (630 giá trị của 42 dòng đều khớp). Sau `--apply`, script chỉ kiểm tra các dòng vừa chuyển.

**Chuyển lại** (khi chuyển production sang bảng này, để lấy các đơn phát sinh trong lúc chờ): chạy lại `migrate-to-v2.py`; dòng đã chuyển được bỏ qua nhờ `Old Record ID`.
**Hoàn tác:** trong Lark lọc `Old Record ID` không trống rồi xóa các dòng đó. Bảng cũ không bị ảnh hưởng. 3 dòng thử ("safe to delete", Row No. 00001-00003) không phải dữ liệu chuyển, bạn xóa trước khi dùng thật.

## 9. Chuyển production sang bảng này, và cách quay lại

Vercel giữ hai biến quyết định đơn thật ghi vào đâu. Đổi biến chỉ có hiệu lực ở **lần deploy kế tiếp**.

**Bật (theo thứ tự):**
1. Đổi biến ở Production (trong terminal của bạn, hoặc Vercel > Project > Settings > Environment Variables):
   ```
   export PATH="$HOME/.nvm/versions/node/v22.23.1/bin:$PATH"
   cd ~/Projects/nhu-luggage
   printf '%s' 'tblphiUeqDjjPdRL' | vercel env update LARK_BASE_TABLE_ID production --yes
   vercel env add LARK_BOOKINGS_SCHEMA production --value 2
   ```
   Giá trị `2` phải không có khoảng trắng hay xuống dòng, vì code so sánh đúng chữ `2`.
2. Deploy: push code lên `main` (có phần route tự ghi `Submitted at`). Vercel build với biến mới.
3. Khi deploy xong, chạy lại `python3 docs/lark/migrate-to-v2.py` để lấy các đơn phát sinh ở bảng cũ trong lúc chờ (dòng đã chuyển được bỏ qua), rồi `--verify`.
4. Đặt **một đơn thử** trên site thật (`www.stowdanang.com`) và kiểm tra dòng đó có trong bảng `Bookings`; sau đó xóa dòng thử. Đơn thử cũng gửi một tin vào nhóm chat nhân viên, nên báo trước cho họ.
5. Sửa `.env.local` trên máy bạn: `LARK_BASE_TABLE_ID=tblphiUeqDjjPdRL` và `LARK_BOOKINGS_SCHEMA=2`, để khi thử ở local đơn không rơi vào bảng lưu trữ.

**Quay lại bảng cũ** (nếu có sự cố sau khi bật):
```
vercel env update LARK_BASE_TABLE_ID production --yes    # nhập: tblTGHAMqIRiCOgd
vercel env rm LARK_BOOKINGS_SCHEMA production --yes
```
rồi deploy lại. Các dòng đã ghi vào bảng mới trong lúc đó vẫn còn nguyên; chạy `migrate-to-v2.py` theo chiều ngược lại không được hỗ trợ, nên nếu quay lại, hãy báo để tôi xử lý.

**Bảng cũ:** giữ nguyên dữ liệu (43 dòng, rev 386 lúc cất) và chỉ đổi tên thành `Bookings (old, archived)`. Lark không cho ẩn bảng bằng công cụ tôi dùng. Muốn ẩn hẳn khỏi nhân viên thì dùng phân quyền nâng cao của Lark Base (theo tôi hiểu có thể ẩn bảng theo vai trò); việc đó ảnh hưởng quyền của mọi người nên tôi không tự làm.

## 10. Cột Plan End: khách còn dời giờ lấy được tới đâu

Có đơn khách đặt giờ lấy sớm hơn hạn của gói rồi sau đó đổi ý. `Pick-up` là giờ khách chọn, còn `Plan End` là **thời điểm gói đã trả tiền thật sự kết thúc**, nên nhân viên biết ngay khách còn dời được tới đâu mà không phải trả thêm. Cột nằm ngay bên phải `Pick-up`, ở cả view `Grid` và `Active`.

| Gói | Plan End |
|---|---|
| By the Hour | Giờ gửi cộng số giờ đã trả (làm tròn lên). Quá 4 giờ tính như 1 ngày nên là 24 giờ sau giờ gửi |
| By the Day | 24 giờ sau giờ gửi |
| Mini | 7 ngày sau giờ gửi |
| Strand | Cùng giờ, cùng ngày của tháng sau (tháng lịch) |
| Long Stay | Cùng giờ, cùng ngày 4 tháng sau |
| Custom | Hết kỳ tính tiền cuối cùng. Ví dụ 46 ngày là 2 gói Strand nên hết ngày 21/11 dù khách chọn lấy ngày 6/11 |

Tin WhatsApp khách gửi cho Stow cũng có dòng `Plan End Date` ngay dưới `Pick-up`, cùng giá trị này, để khách tự thấy gói của mình kéo dài tới khi nào.

Thêm 60 phút ân hạn sau `Plan End` vẫn không tính thêm (form cũng nói thế với khách). Giá trị do `pricing.ts` tính, cùng bộ máy quyết định giá và giờ lấy tối đa, nên không thể lệch với những gì form nói với khách. Đây là giá trị **ghi sẵn**, không phải công thức Lark: nếu khách trả thêm để gia hạn, nhân viên sửa `Plan End`, còn nếu chỉ đổi `Pick-up` thì `Plan End` giữ nguyên (đúng, vì gói không đổi).

**Dòng đã chuyển từ bảng cũ** (điền ngày 2026-09-20, 37 dòng): tính theo độ dài gói, đúng quy tắc mà cột `Date` cũ của bạn dùng (Mini +7 ngày, Strand +30, Long Stay +120, `2 × 4 months` +240, By the Day +24 giờ, By the Hour theo số giờ trên nhãn cũ). Mỗi dòng ghi thêm vào `Migration Note`. **5 dòng để trống** vì không đủ dữ liệu để tính chắc: `STW-260810-5445` (nhãn "Min 1 hr, billed per hr" không có số giờ), `STW-260812-2784` dòng gia hạn (nhãn chỉ là giờ gia hạn, tính ra sớm hơn cả giờ khách đã lấy), `Doanh thu cũ`, `Noname`, `Noname2` (không có giờ gửi). Script: `migrate-to-v2.py --add-plan-end [--apply]`. Bản sao lưu bảng trước khi điền nằm ở `~/stow-lark-backup/migration-2026-09-20/`.

